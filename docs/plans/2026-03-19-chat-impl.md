# 聊天功能实现计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 在牌桌左下角添加聊天框，支持文字和emoji聊天，消息通过服务器中转，房间解散后清除消息。

**Architecture:** 前端新增 ChatBox 组件，后端在 index.js 添加聊天事件处理，RoomManager 管理房间消息存储。

**Tech Stack:** React, Socket.IO

---

## 任务概览

| 任务 | 描述 |
|------|------|
| 1 | 后端：index.js 添加聊天事件处理 |
| 2 | 后端：RoomManager.js 添加消息存储 |
| 3 | 前端：新建 ChatBox.jsx 组件 |
| 4 | 前端：Table.css 添加聊天框样式 |
| 5 | 前端：App.jsx 传入聊天回调函数 |
| 6 | 前端：Table.jsx 引入 ChatBox 组件 |

---

## Task 1: 后端 - index.js 添加聊天事件处理

**Files:**
- Modify: `server/src/index.js`

**Step 1: 添加聊天事件处理**

在 `socket.on('disconnect', ...)` 之前添加：

```javascript
// ---- 聊天事件 ----
socket.on('chat:send', ({ roomId, content }) => {
  try {
    const room = manager.getRoom(roomId)
    if (!room) return
    const player = room.players.find(p => p.id === playerId)
    if (!player || !content?.trim()) return

    const message = {
      id: Date.now().toString(36) + Math.random().toString(36).substr(2),
      playerId,
      playerName: player.name,
      content: content.trim(),
      timestamp: Date.now()
    }

    // 存储到房间消息列表
    room.addMessage(message)

    // 广播给同房间所有玩家
    io.to(roomId).emit('chat:receive', message)
  } catch (e) {
    console.error('chat:send error:', e)
  }
})
```

---

## Task 2: 后端 - RoomManager.js 添加消息存储

**Files:**
- Modify: `server/src/game/RoomManager.js`

**Step 1: 添加消息存储逻辑**

1. 在 RoomManager 类中添加消息存储属性：

```javascript
constructor() {
  this.rooms = new Map()
  this.playerRoom = new Map()
}
```

修改为：

```javascript
constructor() {
  this.rooms = new Map()
  this.playerRoom = new Map()
  this.chatMessages = new Map() // roomId -> messages[]
}
```

2. 添加消息相关方法（在 RoomManager 类中）：

```javascript
// 获取房间消息历史
getRoomMessages(roomId) {
  return this.chatMessages.get(roomId) || []
}

// 添加消息到房间
addRoomMessage(roomId, message) {
  if (!this.chatMessages.has(roomId)) {
    this.chatMessages.set(roomId, [])
  }
  const messages = this.chatMessages.get(roomId)
  messages.push(message)
  // 限制最多50条
  if (messages.length > 50) {
    messages.shift()
  }
}

// 清除房间消息（房间解散时调用）
clearRoomMessages(roomId) {
  this.chatMessages.delete(roomId)
}
```

3. 修改 joinRoom 方法，在玩家加入时返回历史消息：

在 `joinRoom` 方法返回之前添加：

```javascript
const messages = this.getRoomMessages(room.roomId)
return { ...room.getPublicState(playerId), messages }
```

4. 修改 leaveRoom 方法，在房间为空时清除消息：

在 `leaveRoom` 方法中，房间玩家为0时：

```javascript
if (room.players.length === 0) {
  this.clearRoomMessages(roomId)
  this.rooms.delete(roomId)
}
```

---

## Task 3: 前端 - 新建 ChatBox.jsx 组件

**Files:**
- Create: `client/src/components/ChatBox.jsx`

**Step 1: 创建聊天组件**

```jsx
import { useState, useEffect, useRef } from 'react'

const EMOJIS = ['😀', '😃', '😄', '😁', '🤣', '😂', '🙂', '🙃', '😉', '😍', '🤔', '😎', '🥳', '😢', '😡', '😱', '👋', '👍', '👎', '❤️']

export default function ChatBox({ messages, onSendMessage, myId }) {
  const [input, setInput] = useState('')
  const [showEmoji, setShowEmoji] = useState(false)
  const messagesEndRef = useRef(null)

  // 自动滚动到底部
  useEffect(() => {
    messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' })
  }, [messages])

  const handleSend = () => {
    if (!input.trim()) return
    onSendMessage(input)
    setInput('')
    setShowEmoji(false)
  }

  const handleKeyPress = (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault()
      handleSend()
    }
  }

  const insertEmoji = (emoji) => {
    setInput(prev => prev + emoji)
  }

  const formatTime = (timestamp) => {
    const date = new Date(timestamp)
    return date.toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' })
  }

  return (
    <div className="chat-box">
      <div className="chat-messages">
        {messages.length === 0 ? (
          <div className="chat-empty">暂无消息</div>
        ) : (
          messages.map(msg => (
            <div key={msg.id} className={`chat-message ${msg.playerId === myId ? 'my-message' : ''}`}>
              <div className="chat-message-header">
                <span className="chat-sender">{msg.playerName}</span>
                <span className="chat-time">{formatTime(msg.timestamp)}</span>
              </div>
              <div className="chat-content">{msg.content}</div>
            </div>
          ))
        )}
        <div ref={messagesEndRef} />
      </div>

      <div className="chat-input-area">
        <div className="chat-input-row">
          <input
            type="text"
            className="chat-input"
            placeholder="说点什么..."
            value={input}
            onChange={(e) => setInput(e.target.value)}
            onKeyPress={handleKeyPress}
          />
          <button className="chat-emoji-btn" onClick={() => setShowEmoji(!showEmoji)}>😀</button>
          <button className="chat-send-btn" onClick={handleSend}>发送</button>
        </div>

        {showEmoji && (
          <div className="chat-emoji-panel">
            {EMOJIS.map(emoji => (
              <button key={emoji} className="emoji-btn" onClick={() => insertEmoji(emoji)}>{emoji}</button>
            ))}
          </div>
        )}
      </div>
    </div>
  )
}
```

---

## Task 4: 前端 - Table.css 添加聊天框样式

**Files:**
- Modify: `client/src/components/Table.css`

**Step 1: 添加聊天框样式**

在文件末尾（`@keyframes winnerPop` 之后）添加：

```css
/* 聊天框 */
.chat-box {
  position: fixed;
  left: 20px;
  bottom: 20px;
  width: 280px;
  height: 320px;
  background: rgba(20, 35, 20, 0.95);
  border-radius: 12px;
  border: 1px solid rgba(255, 255, 255, 0.1);
  display: flex;
  flex-direction: column;
  z-index: 90;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
  overflow: hidden;
}

.chat-messages {
  flex: 1;
  overflow-y: auto;
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.chat-empty {
  color: rgba(255, 255, 255, 0.4);
  text-align: center;
  font-size: 0.85rem;
  margin-top: auto;
  margin-bottom: auto;
}

.chat-message {
  background: rgba(255, 255, 255, 0.08);
  border-radius: 8px;
  padding: 6px 10px;
  max-width: 85%;
}

.chat-message.my-message {
  background: rgba(46, 125, 50, 0.4);
  margin-left: auto;
}

.chat-message-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 2px;
  gap: 8px;
}

.chat-sender {
  font-size: 0.75rem;
  font-weight: bold;
  color: #f4c430;
}

.chat-message.my-message .chat-sender {
  color: #2ecc71;
}

.chat-time {
  font-size: 0.65rem;
  color: rgba(255, 255, 255, 0.5);
}

.chat-content {
  font-size: 0.85rem;
  color: #fff;
  word-break: break-word;
}

.chat-input-area {
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  padding: 8px;
}

.chat-input-row {
  display: flex;
  gap: 6px;
  align-items: center;
}

.chat-input {
  flex: 1;
  background: rgba(0, 0, 0, 0.3);
  border: 1px solid rgba(255, 255, 255, 0.2);
  border-radius: 6px;
  padding: 6px 10px;
  color: #fff;
  font-size: 0.85rem;
  outline: none;
}

.chat-input:focus {
  border-color: rgba(244, 196, 48, 0.5);
}

.chat-emoji-btn {
  background: rgba(255, 255, 255, 0.1);
  border: none;
  border-radius: 6px;
  width: 32px;
  height: 32px;
  cursor: pointer;
  font-size: 1rem;
  transition: background 0.2s;
}

.chat-emoji-btn:hover {
  background: rgba(255, 255, 255, 0.2);
}

.chat-send-btn {
  background: linear-gradient(135deg, #27ae60, #1e8449);
  border: none;
  border-radius: 6px;
  padding: 6px 12px;
  color: #fff;
  font-size: 0.8rem;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.2s;
}

.chat-send-btn:hover {
  transform: translateY(-1px);
  box-shadow: 0 2px 8px rgba(39, 174, 96, 0.4);
}

.chat-emoji-panel {
  display: grid;
  grid-template-columns: repeat(10, 1fr);
  gap: 4px;
  padding: 8px;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 8px;
  margin-top: 8px;
  max-height: 120px;
  overflow-y: auto;
}

.emoji-btn {
  background: transparent;
  border: none;
  font-size: 1.2rem;
  cursor: pointer;
  padding: 4px;
  border-radius: 4px;
  transition: background 0.2s;
}

.emoji-btn:hover {
  background: rgba(255, 255, 255, 0.2);
}
```

---

## Task 5: 前端 - App.jsx 传入聊天回调函数

**Files:**
- Modify: `client/src/App.jsx`

**Step 1: 修改 App.jsx**

1. 在 useGame hook 中获取 socket：

```javascript
import { getSocket } from './hooks/useSocket'
```

2. 在组件中添加发送消息的函数：

```javascript
const socket = getSocket()

const handleSendChat = (content) => {
  if (!roomId || !content.trim()) return
  socket.emit('chat:send', { roomId, content })
}
```

3. 传递给 Table 组件：

```jsx
<Table
  // ... 现有 props
  onSendChat={handleSendChat}
/>
```

---

## Task 6: 前端 - Table.jsx 引入 ChatBox 组件

**Files:**
- Modify: `client/src/components/Table.jsx`

**Step 1: 添加聊天状态和处理函数**

1. 导入 ChatBox：

```javascript
import ChatBox from './ChatBox'
```

2. 在组件中添加消息状态：

```javascript
const [chatMessages, setChatMessages] = useState([])
```

3. 在 socket 订阅中添加聊天消息处理（在 useGame hook 设置的地方）：

首先需要在 Table 中获取 socket，然后在 useEffect 中监听聊天消息：

```javascript
import { getSocket } from '../hooks/useSocket'

// 在组件内部
const socket = getSocket()

useEffect(() => {
  const handleChatReceive = (message) => {
    setChatMessages(prev => [...prev, message])
  }
  socket.on('chat:receive', handleChatReceive)
  return () => socket.off('chat:receive', handleChatReceive)
}, [])
```

4. 在 Table 函数参数中添加 onSendChat：

```javascript
export default function Table({ gameState, myId, roomId, onAction, onStartGame, onReady, onUnready, onLeaveRoom, onSendChat, error }) {
```

5. 在渲染部分添加 ChatBox 组件（在 table-wrapper 内部，action-panel 之后）：

```jsx
{phase === 'WAITING' && (
  <ChatBox
    messages={chatMessages}
    onSendMessage={onSendChat}
    myId={myId}
  />
)}
```

---

## 测试步骤

1. 启动服务器：`npm run dev --prefix server`
2. 启动前端：`npm run dev --prefix client`
3. 打开两个浏览器标签页，分别进入同一房间
4. 在左下角聊天框输入文字/选择emoji发送
5. 验证另一标签页能收到消息
6. 验证消息显示玩家名字和时间
7. 验证一个玩家离开房间后消息仍保留
8. 验证房间解散后（所有玩家离开）消息清除
